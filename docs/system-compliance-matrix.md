# Матрица соответствия системы S.Center

## Назначение

Матрица связывает текущую реализацию прототипа, код, визуальный слой, источники данных, документацию и предоставленную продуктовую модель S.Center. Это главный рабочий документ для проверки того, что уже реализовано, что совпадает с целевой логикой системы, а что должно перейти в будущий этап.

Каждая строка матрицы фиксирует источник данных, пользовательский путь, статус соответствия, расхождения и следующее действие.

Если изменение элемента может затронуть другие компоненты, сценарии, состояния или данные, дополнительно используется `docs/component-impact-map.md`.

Статусы:

- `совпадает` — текущая реализация соответствует описанной продуктовой логике на уровне прототипа.
- `частично совпадает` — базовый сценарий есть, но часть продуктовой логики пока демонстрационная, неполная или требует уточнения.
- `не реализовано` — элемент описан продуктово, но в текущем прототипе отсутствует.
- `требует будущего этапа` — элемент не должен исправляться в рамках текущего документационного прохода и вынесен в roadmap.

## Матрица соответствия

| Элемент | Назначение | Код логики | Визуальный слой | Источник данных | Текущая документация | Пользовательский путь | Соответствие | Расхождения | Следующее действие |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Левое дерево навигации | Выбор структурного контекста: бизнес-юнит, проект, очередь. | `src/app/app.js`, `src/data/project-structure.js` | `src/components/navigation-tree-item/`, общие стили сайдбара | `window.projectStructureData`, `window.activeContext` | `src/components/navigation-tree-item/README.md`, `docs/context-behavior.md`, `docs/access-control.md` | Пользователь выбирает узел, после чего меняются шапка, лента событий и метрики. | `частично совпадает` | Контекстное переключение реализовано, но нет реальной ролевой видимости, этапов и объектов строительства как активных уровней. | Вынести роли, права и глубокие уровни дерева в `docs/plans/system-scaling-roadmap.md`. |
| Шапка проекта / контекста | Показывает текущий уровень данных и ключевые атрибуты объекта. | `src/app/app.js`, `src/data/project-structure.js` | Разметка шапки в `src/app/app.js`, стили в `src/styles/` | `projectStructureData`, активный контекст | `src/app/README.md`, `docs/context-behavior.md` | Пользователь сразу видит выбранный проект, очередь или бизнес-юнит и его атрибуты. | `частично совпадает` | Шапка перестраивается, но изображения и атрибуты остаются демонстрационными; ролевой состав атрибутов не реализован. | Уточнить целевые атрибуты для бизнес-юнита, проекта и очереди в модели данных. |
| Тулбар событий | Управляет вкладками периода, поиском, фильтрами и запуском аналитики. | `src/app/app.js`, `src/components/event-toolbar/` | `src/components/event-toolbar/event-toolbar.css` | `window.filterState`, `window.toolbarState`, текущая выборка событий | `src/components/event-toolbar/README.md`, `src/features/filters/README.md`, `src/features/analytics/README.md` | Пользователь переключает "Сегодня", "Все события", "Закрепленные", ищет события и запускает анализ. | `частично совпадает` | Базовые действия есть, но нет полноценного date picker и реального backend-поиска по большим массивам. | На будущем этапе отделить UI тулбара от сценариев поиска и анализа. |
| Табы приоритета | Переключают отображение событий высокого и низкого приоритета. | `src/app/app.js` | Стили табов в `src/styles/` и компонентных стилях ленты | `window.filterState.priorityView`, отфильтрованные события | `src/app/README.md`, `docs/component-map.md` | Пользователь выбирает высокий или низкий приоритет и видит соответствующую ленту карточек. | `частично совпадает` | Реализованы табы, но целевая продуктовая логика описывает низкий приоритет как менее заметный список; AI-приоритизация пока демонстрационная. | Зафиксировать будущий выбор: табы остаются как UX-решение или возвращается сворачиваемая модель. |
| Карточка события | Кратко показывает событие в центральной ленте и дает быстрые действия. | `src/app/app.js`, `src/components/event-card/` | `src/components/event-card/event-card.css`, `src/styles/event-list.css` | `appData.getEventListViewModel()` | `src/components/event-card/README.md`, `docs/entity-model.md`, `docs/component-states.md` | Пользователь читает заголовок и описание, открывает детали, закрепляет или исключает событие из текущей выборки. | `частично совпадает` | Краткое представление реализовано, но реальные AI-заголовки, множественное влияние на метрики и обучение приоритета отсутствуют. | Развивать через модель события и будущий AI/ML-этап. |
| Детальная карточка события | Раскрывает расширенный состав события, вывод ИИ и действия пользователя. | `src/app/app.js` | Drawer-разметка и стили в `src/app/app.js`, `src/styles/`, `src/components/event-card/` | `appData.getEventDetailViewModel()` | `src/app/README.md`, `src/components/event-card/README.md`, `docs/decision-actions.md` | Пользователь открывает событие, читает детали, оценивает влияние и выбирает управленческое действие. | `частично совпадает` | Визуальный drawer есть, но действия не интегрированы с реальными системами; ML-инсайт и вывод ИИ демонстрационные. | Описать реальные действия, статусы и историю решений в roadmap. |
| Фильтры событий | Формируют экранную выборку внутри активного структурного контекста. | `src/app/app.js`, `src/features/filters/` | Filter drawer в `src/app/app.js`, стили фильтра | `appData.getEventSources()`, `appData.getMetricSelectorTree()`, `window.filterState` | `src/features/filters/README.md`, `docs/context-behavior.md` | Пользователь выбирает период, источник, приоритет, метрику и применяет фильтр перед анализом. | `частично совпадает` | Атрибутные фильтры есть, но нет полноценного выбора диапазона дат и реального поиска по базе. | На следующем этапе отделить фильтры отображения от контекста для AI-анализа. |
| AI-аналитика | Дает сводный вывод по текущей очищенной выборке событий. | `src/app/app.js`, `src/features/analytics/` | AI-модалка и связанные стили | Текущая выборка событий, `window.aiInsights` | `src/features/analytics/README.md`, `docs/decision-actions.md` | Пользователь фильтрует события, исключает лишнее и запускает анализ. | `частично совпадает` | Сценарий отображается как прототип; реальной AI/ML-модели, массового анализа и периодических выводов нет. | Реализовывать отдельным AI/ML-этапом. |
| Строительные показатели | Показывают строительные метрики выбранного контекста. | `src/app/app.js`, `src/data/app-data.js` | `src/components/construction-metric-card/` | `appData.getConstructionMetricsForContext()` | `src/components/construction-metric-card/README.md`, `docs/plans/construction-metrics-context-plan.md` | Пользователь видит длительность и объем проекта/очереди/бизнес-юнита. | `частично совпадает` | Контекстные значения есть, но набор показателей ограничен двумя карточками; ТЭП и стадии РНС/ЗОС/РВЭ не раскрыты полностью. | Расширить строительную модель и ТЭП в будущем этапе данных. |
| Финансовые метрики | Показывают расчетные показатели выбранного контекста и дочерний уровень дерева. | `src/app/app.js`, `src/data/app-data.js` | `src/components/metric-card/`, правая панель | `window.metricsData`, `appData.getMetricsForContext()` | `src/components/metric-card/README.md`, `src/features/metrics/README.md`, `docs/entity-model.md` | Пользователь смотрит фокусную метрику, проваливается в дочерние и открывает BI. | `частично совпадает` | Контекстный расчет демонстрационный; нет отдельного источника арифметики, реального финансового факта и прав видимости. | Вынести арифметику, реальные значения и права доступа в future roadmap. |
| Структура метрик | Позволяет выбрать любую метрику из дерева и синхронизировать правую панель. | `src/app/app.js`, `src/features/metrics/` | Drawer структуры метрик | `window.metricsData`, `window.activeMetricId` | `src/features/metrics/README.md`, `docs/entity-model.md` | Пользователь открывает дерево, выбирает метрику и видит ее в правой панели и BI. | `частично совпадает` | Дерево есть, но математический источник и ролевое скрытие финансовых веток не вынесены отдельно. | Создать отдельный источник правил расчета и прав видимости. |
| BI-модалка | Показывает расширенную динамику выбранной метрики. | `src/app/app.js`, `src/features/bi/` | BI modal, Chart.js | Выбранная финансовая метрика | `src/features/bi/README.md` | Пользователь открывает график для анализа метрики за жизненный цикл проекта. | `частично совпадает` | График демонстрационный; нет реальной истории, бюджета и прогноза по месяцам жизненного цикла. | Вынести в этап BI-данных и интеграций. |
| Floating action bar | Показывает состояние активной выборки и дает быстрый выход из режима фильтрации. | `src/app/app.js`, `src/components/floating-action-bar/` | `src/components/floating-action-bar/floating-action-bar.css` | `window.toolbarState`, `window.filterState` | `src/components/floating-action-bar/README.md` | Пользователь видит, что работает с отфильтрованной выборкой, и может сбросить режим. | `частично совпадает` | Работает как UI прототипа; целевые правила передачи очищенной выборки в AI нужно закрепить отдельно. | Связать с будущим сценарием массового AI-анализа. |
| `src/data` | Единый слой демонстрационных данных, расчетов и представлений. | `src/data/app-data.js`, `src/data/project-structure.js` | Не имеет собственного визуального слоя | `window.appData`, `window.projectStructureData` | `src/data/README.md`, `docs/entity-model.md` | Компоненты получают события, метрики и контекст из одного источника. | `частично совпадает` | Слой создан, но данные демонстрационные; справочники, интеграции и роли пока не выделены как реальные источники. | Разделить demo data, reference data, calculated data и integration data. |
| `src/app` | Связывает данные, компоненты, активный контекст и глобальные сценарии. | `src/app/app.js` | Рендерит часть крупных областей интерфейса | `window.*`, `window.appData`, `window.projectStructureData` | `src/app/README.md`, `docs/app-decomposition-map.md` | Пользовательские действия приводят к перерисовке шапки, ленты, метрик, drawer и модалок. | `частично совпадает` | Слой выполняет роль orchestration, но остается монолитным; не все сценарии вынесены в компоненты/features. | Декомпозировать малыми этапами после стабилизации документации и данных. |

## Сводка расхождений

Главная логика прототипа уже совпадает с направлением продукта: активный контекст управляет шапкой, событиями и метриками; события являются главным операционным фокусом; метрики выступают вторым аналитическим слоем.

Ключевые расхождения относятся не к визуальной концепции, а к уровню зрелости системы:

- реальные роли и права доступа описаны продуктово, но не реализованы;
- AI/ML-аналитика пока является демонстрационной;
- внешние источники S.Pass, Gantt, Control не подключены;
- периодические аналитические события не генерируются;
- арифметика дерева метрик не вынесена в отдельный canonical source;
- множественное влияние одного события на несколько метрик пока не реализовано как модель данных;
- история управленческих действий и changelog решений отсутствуют;
- строительные ТЭП и глубокие уровни объекта требуют будущего этапа.

Все пункты из этой сводки имеют статус `требует будущего этапа` и вынесены в `docs/plans/system-scaling-roadmap.md`.

## Правило обновления

Матрицу нужно обновлять при любом изменении, которое затрагивает назначение компонента, источник данных, визуальный слой, пользовательский путь, публичный `window.*` интерфейс, состояния компонента, расчетную логику или соответствие текущей реализации предоставленной продуктовой модели.
