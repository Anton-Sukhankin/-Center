# Roadmap масштабирования S.Center

Документ является roadmap будущей реализации и подчиняется регламенту `docs/plans/README.md`, но не заменяет конкретные планы задач.

## Назначение

Документ фиксирует будущую реализацию тех элементов системы, которые уже описаны продуктово, но пока не реализованы полностью в текущем прототипе. Roadmap не заменяет текущую матрицу соответствия, а продолжает ее: все элементы со статусом `требует будущего этапа` должны иметь здесь понятное место.

## Открытые продуктовые решения

- какие управленческие действия, кроме локальной задачи, входят в первую промышленную версию;
- где и с каким workflow хранятся запросы объяснений, задачи и история действий;
- как формализуются пороги значимости и высокий приоритет событий;
- какие источники и владельцы подтверждают финансовые, строительные и календарные данные;
- какие права действуют на уровнях бизнес-юнита, проекта и очереди;
- как изменение пользователем приоритета влияет на будущую AI/ML-приоритизацию.

До утверждения решений текущие demo-правила не считаются промышленной моделью.

## Нереализованные или неполные элементы

| Область | Что уже есть | Что требуется в будущем |
| --- | --- | --- |
| Ролевая адаптация интерфейса | Документация по ролям и ограничениям. | Реальная модель ролей, скрытие веток дерева, метрик, событий и действий по правам пользователя. |
| Права доступа | Описана целевая логика видимости. | Проверка доступа к бизнес-юнитам, проектам, очередям, финансовым метрикам и событиям. |
| Будущие разделы header | `Трекер задач`, `ГПР и Расписание`, `Процессные отчеты` видимы как отключенные пункты `Скоро` без маршрутов. | Для каждого раздела отдельно утвердить назначение, данные, права, маршрут и критерии приемки, после чего заменить disabled-state рабочим сценарием. |
| AI/ML-аналитика | Демонстрационный сценарий анализа. | Реальная индивидуальная и массовая AI-аналитика, выводы, рекомендации и оценка рисков. |
| Периодические аналитические события | Описаны как целевая сущность. | Генерация ежедневных, недельных и месячных агрегированных событий. |
| Интеграции S.Pass, Gantt, Control | Источники представлены как справочник в demo data. | Подключение реальных внешних данных, нормализация событий, обработка отсутствия ожидаемых фактов. |
| Арифметика дерева метрик | Расчет частично реализован в `src/data/app-data.js`. | Отдельный источник правил сложения, вычитания, знаков влияния и агрегации. |
| Центральный дашборд метрик | Общая оболочка, `Общие` и `Закупки` реализованы в vanilla-runtime. Закупочный demo-набор содержит 19 метрик в четырех сегментах, трассировку кратких UI-алиасов к первоисточнику, согласованный агрегат риска и явную маркировку отсутствия пересчета вне проекта-источника. Формулы сохранены как контекст, но runtime их не исполняет. | Выполнить `docs/plans/metrics-real-data-integration-plan.md`: согласовать источники и владельцев, периодичность, базу финансового эффекта, точность, нормы, порог и статусы риска, роли, остальные состояния и промышленную агрегацию. |
| Цифровая шахматка | Нативный demo-feature интегрирован в активный прототип: общий header, полноширинный экран, пять объектов, календарь, 27 работ, матрицы и сравнение. | После продуктовой приемки отдельно подключать реальные источники, методику готовности, права и persistence. |
| Объекты | Нативный demo-feature интегрирован в активный прототип через dropdown: общий selector, параметры, двухуровневая таблица, фильтры, CSV и project/queue/BU semantics. | После продуктовой приемки отдельно определить реальные источники, единую геометрию, `LegalEntity`, методику готовности, row-actions, права, persistence и промышленный экспорт. |
| Сводка цифровой шахматки | Нативный demo-feature интегрирован в активный прототип: пункт `Сводка`, canonical project context, KPI, недельная динамика, проблемы, архив, печать, detail-drawer и общий construction-table contract. Локальный стенд удален после переноса актуального документального и QA-контекста. | Подключать реальные источники, методику KPI/рисков, серверный архив, права и persistence отдельным этапом. |
| Изолированная правая панель и дерево метрик | Карточки строительных/финансовых показателей и drawer структуры сохранены в `legacy/isolated/right-metrics-panel/`. BI-график частично возвращен отдельно в `src/features/bi/` только для event drawer; архивная BI-копия не подключается. | Возвращать панель или дерево отдельным планом и проверить конфликт с центральным режимом `Метрики`, активным BI, ассистентом, фильтрами и будущей ролевой видимостью. Для BI отдельно подключить реальную историю и прогноз. |
| Множественное влияние события | Есть один `metricId` и демонстрационное влияние. | Поддержка нескольких метрик на одно событие и разделение прямого/глобального влияния. |
| Задачи и история управленческих действий | Создание, редактирование и удаление Task из события работают в памяти страницы; есть 14 demo-ответственных с фото-аватарами, карточки задач и индикатор связи. Массовое создание в floating bar показано как отключенное действие `Скоро`. | Постоянное хранение и API, реальный справочник команд проектов, workflow статусов, история изменений, права назначения, запросы, связи с подрядчиками и решения пользователя; batch-create проектировать отдельным подтверждаемым сценарием. |
| Changelog решений | Есть проектные документы и планы. | Единый журнал продуктовых и системных решений с причиной, датой и затронутыми компонентами. |
| Ассистент S.Center / ИИ-чат | Ядро и адаптер находятся в `src/features/chat/`; работают mount root, trigger-кнопка, drawer, одна сессия, ожидание и demo-ответы. | Реальный provider сообщений, права доступа к контексту, серверная история, источники характеристик/календаря и подтверждаемые action-кнопки. |

## Этапы масштабирования

| Этап | Цель | Что сделать | Результат |
| --- | --- | --- | --- |
| 1. Документационная стабилизация | Закрепить связь между продуктовой моделью, кодом и текущим прототипом. | Поддерживать `docs/system-compliance-matrix.md`, паспорта компонентов, feature-документацию, `docs/entity-model.md` и `PROJECT_GUIDELINES.md`. | Любое изменение компонента имеет понятное место в документации. |
| 2. Модель данных | Отделить демонстрационные данные от целевых сущностей и будущих справочников. | Разделить события, метрики, справочники источников, справочники действий, роли, права, структуру объектов и расчетные представления. | `src/data` становится устойчивым источником данных и правил для интерфейса. |
| 3. UI-компоненты | Укрепить внутреннюю библиотеку компонентов. | Выделить повторяемые элементы карточек, строк деревьев, метрик, тегов, пустых состояний и action-панелей. | Компоненты можно переиспользовать без копирования логики и стилей. |
| 3.1. Возврат правого блока метрик | Вернуть изолированный модуль только если он снова нужен продуктово. | Сформировать отдельный план, перенести файлы из `legacy/isolated/right-metrics-panel/`, вернуть подключения и разметку, заменить stubs на активный render, обновить документацию и провести визуальную приемку. | Правый блок метрик снова становится активной частью интерфейса или остается изолированным без влияния на текущий прототип. |
| 3.2. Центральный дашборд метрик | Заменить реализованный demo-контур `Общие` и `Закупки` промышленными источниками и утвержденной методикой. | Выполнить `docs/plans/metrics-real-data-integration-plan.md`; сохранить подтвержденный UI-контракт и заменить статические значения расчетным provider только после согласования входных данных и правил. | Пользователь получает показатели с известным источником, методикой, состоянием качества данных и правами доступа. |
| 3.3. Цифровая шахматка | Подключить реальные данные и права к принятому demo-feature. | Текущая реализация и evidence описаны в feature-паспорте и `docs/evidence/digital-chessboard/README.md`; история интеграции находится в архиве планов. | Пользователь открывает шахматку с промышленными данными без потери состояния соседних экранов. |
| 3.4. Объекты | Перевести принятое demo-представление объектов на промышленный контур. | Утвердить источники, единую геометрию, `LegalEntity`, методику готовности, row-actions, права, persistence и промышленный экспорт. | Пользователь работает с иерархией объектов и работ на подтвержденных данных в пределах своих прав. |
| 3.5. Сводка цифровой шахматки | Перевести принятую demo-сводку на промышленный контур. | Подключить реальные источники, утвердить методику KPI и рисков, серверный архив, права и persistence. | Пользователь получает проверяемую проектную сводку с прозрачной методикой и источниками. |
| 4. Расчетная логика | Сделать метрики и влияние событий методологически корректными. | Вынести правила арифметики дерева, поддержку знаков, агрегацию очередь → проект → бизнес-юнит и множественное влияние событий. | Финансовые и строительные показатели считаются из прозрачных правил. |
| 5. AI/ML | Перейти от демонстрационных выводов к реальной аналитике. | Реализовать индивидуальный анализ события, массовый анализ выборки, периодические выводы и рекомендации действий. | Система помогает пользователю переходить от события к управленческому решению. |
| 5.1. Ассистент S.Center / ИИ-чат | Перевести активный demo-чат на промышленный контур. | Выполнить `docs/plans/ai-chat-provider-actions-plan.md`: provider, безопасность, хранение, источники данных и подтверждаемые actions. | Пользователь задает вопросы по текущему контексту и безопасно выполняет только явно подтвержденные действия. |
| 6. Роли и доступы | Адаптировать интерфейс под пользователя. | Настроить видимость дерева, метрик, событий, фильтров и действий для РП, топ-менеджмента, планировщиков, контрольных служб и подрядчиков. | Пользователь видит только релевантные и разрешенные данные. |
| 7. Интеграции | Подключить реальные продуктовые источники. | Интегрировать S.Pass, Gantt, Control и другие системы; нормализовать события и источники. | Лента событий становится реальным операционным контуром. |
| 8. Управленческий контур | Перевести текущий in-memory Task-прототип в постоянный контур действий и истории решений. | Подключить хранение и API, workflow статусов, реальный справочник и права ответственных; хранить запросы объяснений, изменения приоритета и исключения из AI-выборки. | Система сохраняет не только проблему, но и контролируемую реакцию на неё между сессиями. |

## Правило переноса из матрицы

Если в `docs/system-compliance-matrix.md` элемент получает статус `не реализовано` или `требует будущего этапа`, у него должно быть следующее действие в этом roadmap. Если элемент переходит в реализацию, соответствующий этап roadmap нужно уточнить, а после выполнения обновить матрицу соответствия и связанные паспорта компонентов.

## Ближайшие приоритеты

1. Закрепить демонстрационную модель данных как отдельный слой и явно отделить ее от будущих справочников.
2. Уточнить, какие компоненты должны стать базовыми элементами внутренней UI-библиотеки.
3. Описать целевую модель ролей и прав доступа в формате, пригодном для будущей реализации.
4. Сформировать отдельный документ правил арифметики финансового дерева.
5. Описать контур AI-аналитики: входная выборка, исключенные события, результат анализа, действия пользователя.
