# Матрица соответствия системы S.Center

## Назначение

Матрица связывает текущую реализацию прототипа, код, визуальный слой, источники данных, документацию и предоставленную продуктовую модель S.Center. Это главный рабочий документ для проверки того, что уже реализовано, что совпадает с целевой логикой системы, а что должно перейти в будущий этап.

Каждая строка матрицы фиксирует источник данных, пользовательский путь, статус соответствия, расхождения и следующее действие.

Если изменение элемента может затронуть другие компоненты, сценарии, состояния или данные, дополнительно используется `docs/component-impact-map.md`.

Статусы:

- `совпадает` — текущая реализация соответствует описанной продуктовой логике на уровне прототипа.
- `частично совпадает` — базовый сценарий есть, но часть продуктовой логики пока демонстрационная, неполная или требует уточнения.
- `не реализовано` — элемент описан продуктово, но в текущем прототипе отсутствует.
- `требует будущего этапа` — элемент не должен исправляться в рамках текущего документационного прохода и вынесен в roadmap.

## Матрица соответствия

| Элемент | Назначение | Код логики | Визуальный слой | Источник данных | Текущая документация | Пользовательский путь | Соответствие | Расхождения | Следующее действие |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Левое дерево навигации | Выбор структурного контекста: бизнес-юнит, проект, очередь. | `src/app/app.js`, `src/data/project-structure.js` | `src/components/navigation-tree-item/`, общие стили сайдбара | `window.projectStructureData`, `window.activeContext` | `src/components/navigation-tree-item/README.md`, `docs/context-behavior.md`, `docs/access-control.md` | Пользователь выбирает узел, после чего меняются шапка, лента событий и метрики. | `частично совпадает` | Контекстное переключение реализовано, но нет реальной ролевой видимости, этапов и объектов строительства как активных уровней. | Вынести роли, права и глубокие уровни дерева в `docs/plans/system-scaling-roadmap.md`. |
| Шапка проекта / контекста | Показывает текущий уровень данных и ключевые атрибуты объекта. | `src/app/app.js`, `src/data/project-structure.js` | Разметка шапки в `src/app/app.js`, стили в `src/styles/` | `projectStructureData`, активный контекст | `src/app/README.md`, `docs/context-behavior.md` | Пользователь сразу видит выбранный проект, очередь или бизнес-юнит и его атрибуты. | `частично совпадает` | Шапка перестраивается, но изображения и атрибуты остаются демонстрационными; ролевой состав атрибутов не реализован. | Уточнить целевые атрибуты для бизнес-юнита, проекта и очереди в модели данных. |
| Тулбар событий | Управляет вкладками периода, режимом `Метрики`, поиском, фильтрами, запуском аналитики и входом в ассистента. | `src/app/app.js`, `src/components/event-toolbar/` | `src/components/event-toolbar/event-toolbar.css`; в режиме метрик центральная зона пустая без дублирующей подписи проекта, а строка внутренних вкладок метрик примыкает к тулбару на белой полноширинной подложке и повторяет стиль табов приоритета | `window.filterState`, `window.toolbarState`, текущая выборка событий | `src/components/event-toolbar/README.md`, `src/features/filters/README.md`, `src/features/analytics/README.md`, `src/features/chat/README.md`, `src/app/README.md` | Пользователь переключает события и `Метрики`; в режиме метрик поиск, аналитика и фильтр событий скрываются, а `Задать вопрос` остается доступной. | `частично совпадает` | Контекстный режим реализован; нет полноценного date picker и реального backend-поиска по большим массивам. | Согласовать отдельные аналитические действия для метрик после подключения реальных источников. |
| Центральный дашборд метрик | Показывает контрольные этапы, сроки и бюджетные основания выбранного проекта или очереди в центральной области. | `src/features/metrics-dashboard/`, `src/app/app.js` | Центральный dashboard с внутренними вкладками, KPI, контрольными этапами, сроками и бюджетными карточками; project/queue context обновляется без потери shell. | `window.metricsDashboardData`, `window.metricsDashboardViewModel`, `window.activeContext` | `src/features/metrics-dashboard/README.md`, `docs/plans/metrics-real-data-integration-plan.md`, `src/app/README.md`, `src/data/README.md`, `docs/evidence/metrics-dashboard/` | Пользователь выбирает проект или очередь, открывает `Метрики`, изучает demo-показатели, разворачивает/сворачивает этапы и переключает внутренние разделы. | `частично совпадает` | UI и demo-данные реализованы; реальные источники, утвержденная методика, роли и агрегация бизнес-юнита отсутствуют. | Выполнить `docs/plans/metrics-real-data-integration-plan.md`. |
| Сводка цифровой шахматки | Дает проектный обзор KPI, недельной динамики, проблем и состояния объектов строительства. | `src/features/digital-chessboard-summary/`, `src/components/construction-table/`, `src/data/digital-chessboard-summary-data.js`, `src/app/app.js`, `index.html` | Полноширинный нативный экран под общим header; sticky toolbar/KPI-shell; шесть горизонтальных KPI-карточек, где icon-tile и крупное значение находятся в верхней строке, а название параметра — ниже; два равновысоких scroll-блока; таблица с общим border/scroll contract раздела `Объекты`; drawer архива/деталей и modal печати на слое `2410`; responsive и print states | `window.digitalChessboardSummaryData`, `window.constructionObjectsData`, `window.objectsData`, `window.activeContext` | `src/features/digital-chessboard-summary/README.md`, `src/components/construction-table/README.md`, `src/data/README.md`, `src/app/README.md`, `docs/reference/digital-chessboard-summary/README.md`, `docs/evidence/digital-chessboard-summary/README.md` | Пользователь выбирает `Сводка` в dropdown, меняет canonical проект, изучает KPI, динамику, проблемы и объекты, использует архив, печать и detail-drawer; queue наследует проект, business unit показывает unsupported-state. | `частично совпадает` | Полный demo-feature интегрирован и принят как самостоятельная main-view; локальный стенд удален после переноса актуального UI/UX-, data- и QA-контекста. KPI/риски/архив детерминированы локально, cross-feature `Открыть` информационный, реальные источники, методика, права и persistence отсутствуют. | Проектировать реальные источники, методику KPI/рисков, серверный архив, права и persistence отдельным этапом. |
| Цифровая шахматка | Показывает готовность выбранной работы по объекту, этажам и секциям и поддерживает попарное сравнение работ. | `src/features/digital-chessboard/`, `src/components/construction-object-selector/`, `src/data/digital-chessboard-data.js`, `src/app/app.js` | Полноширинный экран под общим header; вход через dropdown; пять общих object cards с roving-навигацией; description под названием объекта показывает структурные параметры (`этажи/секции`, `уровни/зоны`) вместо типа объекта, отдельная нижняя meta-строка не выводится; карточка разделена на верхний информационный блок с нижним border и нижний блок из зоны круговой готовности с процентом и зоны пояснения `14px`, отделенных вертикальным border; title рабочей зоны `Готовность секций по этажам` размещен под карточками объектов и над сводкой/действиями и использует служебный стиль `12px / 600`, uppercase, тёмно-серый `#475569`; календарь, sticky-матрица со значениями готовности `12px` и мягким статусным hover, scroll-индикаторы, легенда, empty-state и модальные окна с focus trap; demo-ячейки используют неравномерный детерминированный фронт работ, разные темпы секций, локальные отклонения и смешанный шаг процентов вместо повторяющейся линейной «лесенки»; статусная точка, название выбранной работы и карандаш образуют одну кнопку с единым hover/focus и одной точкой входа в picker; пустой слот сравнения содержит центрированную заглушку с серой круглой подложкой, иконкой `16×16`, title и description; параметры верхней сводки и выбранной работы используют общий контракт: лейбл `12px`, значение `16px / 600`, синхронизированный с разделом `Объекты`; верхняя сводка начинается с параметра `Готовность`, который использует тот же `summary.actual`, что и активная карточка объекта, и обновляется при переключении карточки, затем следуют `План`, `Отклонение` и `Обновлено`; parameter-подблоки расширены на `16px` через горизонтальный padding и стандартизированы по высоте `73px` с вертикальным padding `16px`, без внутренних border-разделителей; вся строка наименования и параметров отделена от матрицы сплошным нижним border; расстояние между сводкой/действиями и матрицей `4px` | `window.constructionObjectsData`, `window.digitalChessboardData`, `window.activeContext` | `src/features/digital-chessboard/README.md`, `src/components/construction-object-selector/README.md`, `src/data/README.md`, `src/app/README.md` | Пользователь выбирает `Шахматка` в dropdown, затем объект, видит title рабочей зоны, выбирает диапазон и работу; возврат между dashboard, шахматкой и `Объектами` сохраняет независимые состояния. | `частично совпадает` | Demo-сценарий интегрирован; источники, методика готовности, роли, persistence и объект в общем дереве остаются будущими. | Подключать реальные источники, права, persistence и согласованную геометрию отдельным этапом. |
| Объекты | Показывает параметры выбранного объекта и иерархию групп/видов работ со сроками, подрядчиками, готовностью, статусами, фильтрацией и выгрузкой. | `src/features/objects/`, `src/components/construction-object-selector/`, `src/data/objects-data.js`, `src/app/app.js`, `index.html` | Полноширинная feature-область с общим selector объектов, параметрами, иерархической таблицей, фильтрами, sticky-областями и CSV. | `window.constructionObjectsData`, `window.objectsData`, `window.activeContext` | `src/features/objects/README.md`, `src/components/construction-object-selector/README.md`, `src/data/README.md`, `src/app/README.md`, `docs/reference/objects/README.md` | Пользователь выбирает объект, фильтрует и раскрывает дерево работ, прокручивает таблицу и формирует CSV; project/queue поддержаны, business unit показывает unsupported-state. | `частично совпадает` | Demo-feature принят; не согласованы реальные источники, методика готовности, права, persistence, промышленный экспорт и row-actions. | Вынести промышленные данные и действия в отдельный подтвержденный план. |
| Табы приоритета | Переключают отображение событий высокого и низкого приоритета. | `src/app/app.js` | Стили табов в `src/styles/` и компонентных стилях ленты | `window.filterState.priorityView`, отфильтрованные события | `src/app/README.md`, `docs/component-map.md` | Пользователь выбирает высокий или низкий приоритет и видит соответствующую ленту карточек. | `частично совпадает` | Реализованы табы, но целевая продуктовая логика описывает низкий приоритет как менее заметный список; AI-приоритизация пока демонстрационная. | Зафиксировать будущий выбор: табы остаются как UX-решение или возвращается сворачиваемая модель. |
| Карточка события | Кратко показывает событие, быстрые действия и наличие связанной задачи. | `src/app/app.js`, `src/components/event-card/` | `src/components/event-card/event-card.css`, `src/styles/event-list.css`; inline-акценты `**...**` отображаются как `font-weight: 500`, line-clamp описания сохранен | `appData.getEventListViewModel()`, `taskData.getTaskCountForEvent()` | `src/components/event-card/README.md`, `docs/entity-model.md`, `docs/component-states.md`, `docs/source-visual-themes.md` | Пользователь читает уникальное смысловое название события, более объемное описание с акцентами, видит `clipboard-check` при наличии задачи, открывает детали, закрепляет или исключает событие. | `частично совпадает` | Индикатор связанной задачи, уникальные demo-заголовки и расширенные demo-описания реализованы, но реальные AI-заголовки, множественное влияние на метрики и обучение приоритета отсутствуют. | Развивать карточку через модель события и будущий AI/ML-этап; постоянную связь задач — через backend-этап. |
| Детальная карточка события | Раскрывает событие, связанные задачи, вывод ИИ, связанную метрику и действия пользователя. | `src/app/app.js`, `src/features/tasks/`, `src/features/bi/` | Viewport-level drawer события поверх всего интерфейса, включая глобальный header: overlay `z-index: 2400`, панель `2401`, окно команды проекта `2402`, BI-график связанной метрики `2403`; `src/styles/`, `src/features/tasks/task-drawer.css`; описание выводит структурированный `detailText`; длинное описание раскрывается кнопкой `Дополнительно`; блок метрики является кнопкой открытия BI; карточка созданной задачи статична, без зеленой обводки и стрелки, с action-кнопками редактирования и удаления | `appData.getEventDetailViewModel()`, `window.taskData`, `window.metricsData` | `src/app/README.md`, `src/components/event-card/README.md`, `src/features/tasks/README.md`, `src/features/bi/README.md`, `docs/decision-actions.md` | Пользователь читает событие, раскрывает описание, создает задачу, редактирует ее кнопкой с карандашом, удаляет кнопкой с корзиной либо открывает BI-график связанной метрики; после закрытия графика drawer и фокус на событии сохраняются, а при закрытии drawer активный BI закрывается каскадно. | `частично совпадает` | Task хранится в памяти, а история BI и прогноз демонстрационные; «Запросить обоснование», backend и постоянное хранение отсутствуют. | Подключить реальные действия, историю метрик, backend и workflow отдельными этапами. |
| Создание, редактирование и удаление задачи | Превращает событие в контролируемое действие с ответственным. | `src/features/tasks/task-drawer.js`, `src/app/app.js`, `src/data/task-data.js` | Drawer создания/редактирования, карточки задач и окно выбора одного demo-ответственного. | `window.taskData`, исходное событие из `window.appData` | `src/features/tasks/README.md`, `docs/entity-model.md`, `docs/decision-actions.md`, `docs/evidence/tasks/` | Пользователь создает задачу из события, выбирает ответственного, затем редактирует или удаляет задачу. | `частично совпадает` | Create/edit/delete работают in-memory; нет backend, workflow, истории, прав и dirty-confirmation. | Отдельно проектировать постоянное хранение, workflow, права и историю. |
| Фильтры событий | Формируют экранную выборку внутри активного структурного контекста. | `src/app/app.js`, `src/features/filters/` | Правый filter drawer шириной `570px`; overlay `z-index: 2400`, панель `2401`, то есть выше глобального header `2300`; header-icon `44px` на `44px` является визуальным эталоном для header-метафоры ассистента | `appData.getEventSources()`, `appData.getMetricSelectorTree()`, `window.filterState` | `src/features/filters/README.md`, `docs/context-behavior.md` | Пользователь выбирает период, источник, приоритет, метрику и применяет фильтр перед анализом. | `частично совпадает` | Атрибутные фильтры есть, но нет полноценного выбора диапазона дат и реального поиска по базе. | На следующем этапе отделить фильтры отображения от контекста для AI-анализа. |
| AI-аналитика | Дает сводный вывод по текущей очищенной выборке событий. | `src/app/app.js`, `src/features/analytics/` | AI-модалка и связанные стили | Текущая выборка событий, `window.aiInsights` | `src/features/analytics/README.md`, `docs/decision-actions.md` | Пользователь фильтрует события, исключает лишнее и запускает анализ. | `частично совпадает` | Сценарий отображается как прототип; реальной AI/ML-модели, массового анализа и периодических выводов нет. | Реализовывать отдельным AI/ML-этапом. |
| AIShtab / ИИ-чат | Диалоговый помощник по текущему контексту S.Center. | `src/features/chat/`, `src/components/event-toolbar/event-toolbar.js` | Правый viewport-level drawer с контекстными быстрыми вопросами, ожиданием ответа и single-session reset. | `window.activeContext`, `window.filterState`, `window.toolbarState`, `window.metricsData`, текущая экранная выборка | `src/features/chat/README.md`, `src/components/event-toolbar/README.md`, `docs/plans/ai-chat-provider-actions-plan.md` | Пользователь открывает drawer кнопкой `Задать вопрос`, отправляет свой или быстрый вопрос и получает demo-ответ с follow-up. | `частично совпадает` | UI и demo-provider подключены; нет промышленного provider, серверной истории, прав и согласованных actions. | Выполнить `docs/plans/ai-chat-provider-actions-plan.md`. |
| Расчетные данные метрик | Сохраняют связь событий, фильтров, ассистента и BI с финансовыми и строительными показателями без правого визуального блока. | `src/app/app.js`, `src/data/app-data.js`, `src/features/bi/` | Правой панели нет; event drawer показывает связанную метрику и открывает отдельный BI-график | `window.metricsData`, `window.activeMetricId`, `appData.getMetricsForContext()`, `appData.getConstructionMetricsForContext()` | `src/data/README.md`, `src/features/bi/README.md`, `docs/entity-model.md`, `docs/component-map.md` | Пользователь видит и фильтрует связь события с метрикой, а из detail-view открывает ее график; правая панель не отображается. | `частично совпадает` | Факт/план/прогноз контекстные demo-значения; месячная история BI генерируется внутри feature. | Подключить реальную историю и методику прогноза отдельным этапом данных. |
| BI-график метрики | Дает детальный просмотр выбранной метрики из события без возврата правой панели. | `src/features/bi/`, `src/app/app.js`, `index.html` | Side-by-side рядом с event drawer на широком экране и modal overlay на узком. | Выбранный узел `window.metricsData`; демонстрационная месячная серия | `src/features/bi/README.md`, `docs/entity-model.md` | Пользователь открывает связанную метрику, изучает план/факт и прогноз, затем возвращается к событию. | `частично совпадает` | Интерактивный сценарий реализован; временной ряд и прогноз демонстрационные. | Подключить реальный источник временного ряда и единицы измерения. |
| Изолированный правый блок метрик | Сохраненная версия правой панели, drawer структуры метрик и архивная исходная версия BI. | Не входит в активный `src`; сохранен в `legacy/isolated/right-metrics-panel/` | Не подключен в `index.html`; активный BI реализован отдельной копией в `src/features/bi/` | Сохраненные компоненты опирались на `window.metricsData`, `appData.getMetricsForContext()`, `appData.getConstructionMetricsForContext()` | `legacy/isolated/right-metrics-panel/README.md`, `legacy/isolated/AGENTS.md`, `.agentsignore` | Путь к правой панели и дереву отсутствует; BI доступен только из event drawer. | `требует будущего этапа` | Правая панель и дерево остаются изолированными; архивная BI-копия не выполняется. | Возвращать панель или дерево только по отдельному плану с учетом активного BI. |
| Floating action bar | Показывает состояние активной выборки и дает быстрый выход из режима фильтрации. | `src/app/app.js`, `src/components/floating-action-bar/` | `src/components/floating-action-bar/floating-action-bar.css` | `window.toolbarState`, `window.filterState` | `src/components/floating-action-bar/README.md` | Пользователь видит, что работает с отфильтрованной выборкой, и может сбросить режим. | `частично совпадает` | Работает как UI прототипа; целевые правила передачи очищенной выборки в AI нужно закрепить отдельно. | Связать с будущим сценарием массового AI-анализа. |
| `src/data` | Единый слой демонстрационных данных, расчетов, представлений и runtime-задач. | `src/data/app-data.js`, `src/data/project-structure.js`, `src/data/task-data.js`, `src/data/construction-objects-data.js`, `src/data/objects-data.js`, `src/data/digital-chessboard-data.js`, `src/data/digital-chessboard-summary-data.js` | Не имеет собственного визуального слоя | `window.appData`, `window.projectStructureData`, `window.taskData`, `window.constructionObjectsData`, `window.objectsData`, `window.digitalChessboardData`, `window.digitalChessboardSummaryData` | `src/data/README.md`, `docs/entity-model.md` | Компоненты получают события, задачи, метрики, контекст и стабильную идентичность объектов из общих источников; feature-specific геометрия и summary view-model не смешиваются. | `частично совпадает` | Все строительные наборы остаются demo; реальные справочники, интеграции, persistence и роли отсутствуют. | Сохранить границу общей identity и feature-specific view-model; реальные API проектировать отдельно. |
| `src/app` | Связывает данные, компоненты, features, активный контекст и глобальные сценарии. | `src/app/app.js` | Рендерит часть крупных областей и управляет четырьмя main-view | `window.*`, `window.appData`, `window.projectStructureData`, `window.taskData`, `window.SCenterTasks`, `window.SCenterDigitalChessboardSummary`, `window.SCenterDigitalChessboard`, `window.SCenterObjects` | `src/app/README.md`, `docs/app-decomposition-map.md` | Пользовательские действия приводят к перерисовке контекста и переключению dashboard / `Сводки` / шахматки / `Объектов` без перемонтирования. | `частично совпадает` | Feature-логика вынесена, но общий orchestration и screen-controller остаются в монолитном classic-script. | Декомпозировать контроллер малыми этапами без изменения публичных `window.*`. |

Уточнение визуального слоя раздела `Объекты`: закрепленные disclosure- и name-ячейки header имеют приоритет слоя над sticky-ячейками строк, поэтому строки и кнопки раскрытия не перекрывают title при вертикальном скролле.

Уточнение визуального слоя dropdown `Цифровая шахматка`: при выборе `Сводка`, `Объекты` или `Шахматка` соответствующая feature-поверхность выводит собственный локальный заголовок раздела в верхней левой части рабочей области. Заголовки используют единый контракт `24px / 600` и не заменяют служебные titles внутренних рабочих блоков.

## Сводка расхождений

Главная логика прототипа уже совпадает с направлением продукта: активный контекст управляет шапкой, событиями и метриками; события являются главным операционным фокусом; метрики выступают вторым аналитическим слоем.

Ключевые расхождения относятся не к визуальной концепции, а к уровню зрелости системы:

- реальные роли и права доступа описаны продуктово, но не реализованы;
- AI/ML-аналитика пока является демонстрационной;
- внешние источники S.Pass, Gantt, Control не подключены;
- периодические аналитические события не генерируются;
- арифметика дерева метрик не вынесена в отдельный canonical source;
- множественное влияние одного события на несколько метрик пока не реализовано как модель данных;
- история управленческих действий и changelog решений отсутствуют;
- строительные ТЭП и глубокие уровни объекта требуют будущего этапа.

Все пункты из этой сводки имеют статус `требует будущего этапа` и вынесены в `docs/plans/system-scaling-roadmap.md`.

## Правило обновления

Матрицу нужно обновлять при любом изменении, которое затрагивает назначение компонента, источник данных, визуальный слой, пользовательский путь, публичный `window.*` интерфейс, состояния компонента, расчетную логику или соответствие текущей реализации предоставленной продуктовой модели.
